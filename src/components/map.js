import React, { Component } from 'react'

class Map extends Component {
  //need to figure our how to display map with location requirements
  //requirements: centered around user location, location of  restaurant click on from bookmarks, with markers of results
  render() {
    return (
      <div className="left">this will be a map</div>
    )
  }
}

export default Map